<div class="container p-5">
  <div class='flex flex-col gap-3'>
    <h1 class="font-bold text-4xl">Feeds</h1>
    <div id="feeds" class="min-w-full">
      <%= @feeds.present? ? render('table', feeds: @feeds) : "You don't have any feeds" %>
    </div>
  </div>
</div>
