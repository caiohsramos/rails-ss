<%= turbo_frame_tag :content, data: { turbo_permanent: true } do %>
  <%= turbo_frame_tag :items, src: folder_items_path(@folder.id, filter: @settings.filter, q: params[:q]), data: { controller: 'content', action: 'turbo:frame-load->content#loaded' } %>
<% end %>
