<div class="container p-5">
  <div class='flex flex-col gap-3'>
    <h1 class="font-bold text-4xl">Folders</h1>
    <div id="folders" class="min-w-full">
      <%= @folders.present? ? render('table', folders: @folders) : "You don't have any folders" %>
    </div>
  </div>
</div>
