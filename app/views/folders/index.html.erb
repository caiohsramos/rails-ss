<div class="container p-5">
  <div class='flex flex-col gap-3'>
    <h1 class="font-bold text-4xl">Folders</h1>
    <div id="folders" class="min-w-full">
      <%= render(partial: 'folder', collection: @folders, spacer_template: "application/divider") || "You don't have any folders" %>
    </div>
  </div>
</div>
